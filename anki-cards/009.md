Как создать URL-маршрут в Django, который подключит view функцию `home_view` из `blog.views` к корневому пути `/`?

%

```python
# config/urls.py или blog/urls.py
from django.urls import path
from blog.views import home_view

urlpatterns = [
    path('', home_view, name='home'),
]
```
