Как зарегистрировать модель в Django Admin, чтобы она была доступна в админ-панели?

%

```python
# admin.py
from django.contrib import admin
from blog.models import Post

admin.site.register(Post)
```
